# 2026-04-24 工作日志

## Thoth CI/CD 全面优化

### Repo 定位
- 用户确认：repo 目的是**提供源码 + 发布 GitHub Release**，不做 Vercel 部署
- CI/CD 唯一目的：tag push → 自动构建 APK → GitHub Release 附带 APK

### CI/CD 最终结构（.github/workflows/）
```
ci.yml         — lint + type-check + test（PR/branch 质量门控）
release.yml    — tag push → CI门控 → assembleDebug → GitHub Release with APK
```
已删除（之前某次操作中已删）：deploy-dev.yml, deploy-prod.yml, build.yml, android.yml

### 涉及文件变更
- `ci.yml` — npm ci 升级（原有内容）
- `release.yml` — 新增，tag 触发型 Release workflow
  - jobs: ci → build-debug → release（softprops/action-gh-release）
  - 注入 VITE_* 环境变量
  - debug APK 作为 Release 附件
  - release AAB 签名部分注释保留，待配置 keystore 后启用
- `docs/ANDROID_PLAN.md` — Phase 3 改为 GitHub Release，CI/CD 章节重建，Phase 0 完成状态更新至 v1.2.0

### GitHub Secrets 待配置（用户手动）
必须配置，否则 CI build 会失败：
- `VITE_FIREBASE_API_KEY`
- `VITE_FIREBASE_AUTH_DOMAIN`
- `VITE_FIREBASE_PROJECT_ID`
- `VITE_FIREBASE_STORAGE_BUCKET`
- `VITE_FIREBASE_MESSAGING_SENDER_ID`
- `VITE_FIREBASE_APP_ID`
- `VITE_GEMINI_API_KEY`
- `VITE_R2_PUBLIC_URL`

暂不需要（release AAB 签名用）：
- `ANDROID_KEYSTORE_BASE64`, `ANDROID_KEY_ALIAS`, `ANDROID_KEY_PASSWORD`, `ANDROID_STORE_PASSWORD`

### 其他阻塞项
- Firebase google-services.json 未放置到 android/app/（需从 Firebase Console 下载）
- Android Studio 未安装（本地开发用，CI 用 GitHub hosted runner 不受影响）

### WearOS 开发计划（本轮新增）
- 新建 `docs/WEAROS_PLAN.md`：多模块架构，phone + wear + common 三层结构
- 核心思路：watch 只做一件事——一键录音，其他全部留在手机
- common/ 模块：共享 Firebase 数据层（Auth + Firestore + Storage）
- wear/ 模块：Jetpack Compose Wear，minSdk 30（Wear OS 3.0）
- 暂不需要 Firebase google-services.json（wear/ 可复用 phone 的，或从 Firebase Console 单独下载）

### Web 重构完成（Phase 0+1+2+3 本轮完成）
- `npm run build` ✅，Capacitor sync ✅
- 新增文件：
  - `src/types/index.ts` - 所有类型（Dream, UserProfile, FirestoreErrorInfo, OperationType, DreamAnalysis）
  - `src/lib/errors.ts` - handleFirestoreError
  - `src/lib/ai.ts` - transcribeDream + analyzeDream（Gemini 调用）
  - `src/lib/storage.ts` - uploadAudio（R2 主 + Firebase Storage 降级）
  - `src/lib/firestore.ts` - syncUserStats + updateGlobalImagery + updateGlobalLocation
  - `src/hooks/useAuth.ts` - 认证 + profile 管理
  - `src/hooks/useDreams.ts` - Firestore 订阅 + CRUD 操作
  - `src/hooks/useRecording.ts` - 录音状态机
  - `src/hooks/useCountdown.ts` - 记忆崩塌倒计时
  - `src/hooks/index.ts` - barrel 导出
  - `src/components/Navigation.tsx` - 顶部导航
  - `src/components/TabBar.tsx` - 移动端底部 Tab
  - `src/components/RecordView.tsx` - 录音 Tab UI
  - `src/components/HistoryView.tsx` - 历史 Tab UI
  - `src/components/GlobalView.tsx` - 全球地图 Tab UI
  - `src/components/SettingsView.tsx` - 设置 Tab UI
  - `src/components/DreamDetailModal.tsx` - 梦境详情弹窗
  - `src/components/DeleteConfirmModal.tsx` - 删除确认弹窗
- `src/App.tsx` 从 1329 行 → ~140 行（薄调度层）
- 构建警告：chunk 1.27MB（D3 + Firebase + Gemini SDK，建议后续加 manualChunks code-split）

### iOS 搭建完成（本轮）
- `npm install @capacitor/ios` ✅
- `npx cap add ios` ✅ → 生成 `ios/` 文件夹
- `ios/App/App/Info.plist` 已添加：NSMicrophoneUsageDescription、NSLocationWhenInUseUsageDescription、UIBackgroundModes（audio）
- `capacitor.config.ts` 已添加 iOS 配置（contentInset、backgroundColor、allowsLinkPreview）
- `npx cap sync ios` ✅
- `docs/IOS_PLAN.md` 已创建
- 阻塞项：必须有一台 Mac 才能用 Xcode 编译 .ipa

### 待开始
- Phase W0: 创建 common/ + wear/ 模块骨架，验证 Gradle 编译通过
- iOS CI/CD：`release.yml` 待新增 `build-ios` job（macOS runner，等 Mac 就位）

### 项目缺失清单（新增）
- 新建 `docs/MISSING_CHECKLIST.md`：全英文，分 Blocker / High / Medium / Nice-to-Have 四级
  - Blocker：Firebase 项目配置、google-services.json（Android）、GoogleService-Info.plist（iOS）、Gemini API Key
  - High：Cloudflare R2 凭证、微信支付商户凭证（backend + frontend + Open Platform 注册）、WeChat SDK 回调签名验证 TODO
  - Medium：GitHub Secrets 配置、Android release keystore、iOS signing certs
  - Nice-to-Have：Firestore rules 增强、Payment server 部署、Apple Developer Program

### 微信支付集成（代码准备完成）
- 商户模式 + App 支付（APIv3）
- 新增文件：
  - `src/lib/payment.ts` — 前端支付服务（createPaymentOrder / openWeChatPay / pay）
  - `src/types/index.ts` — 新增 PaymentStatus / CreateOrderParams / PaymentResult
  - `server.ts` — 新增 3 个路由（create-order / callback / query-order）
  - `android/.../WXEntryActivity.java` — 微信 SDK 回调接收器
  - `android/.../AndroidManifest.xml` — 注册 WXEntryActivity
  - `docs/PAYMENT_INTEGRATION.md` — 全英文集成文档
- 待配置凭证（用户自行完成）：WX_MCHID / WX_SERIAL_NO / WX_PRIVATE_KEY_PATH / WX_APIV3_KEY / WX_APP_ID
- 待实现：回调签名验签（TODO）、订单持久化（当前用 Map 内存）

