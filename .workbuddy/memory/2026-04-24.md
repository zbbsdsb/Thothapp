# 2026-04-24 工作日志

## Thoth CI/CD 全面优化

### 问题修复（P0）
- `deploy-prod.yml`：加入 `needs: ci` 门控，修复"lint 失败照样部署"问题
- `deploy-dev.yml`：移除错误的 `--prod` flag（改为 `--prebuilt`），防止 dev 部署覆盖 production

### 功能优化（P1）
- 全部 workflow 从 `npm install` 改为 `npm ci`（更快、更可靠、确保 lock 一致性）
- `build.yml` + 两个 deploy workflow：注入 `VITE_*` 环境变量（防止 build 产生 Firebase 配置空壳）
- 新增 `android.yml`：Debug APK 构建，支持 `workflow_dispatch` 手动触发，预留 release AAB 签名注释

### Repo 定位确认（重要）
- 用户确认：repo 目的是**提供源码 + 发布 GitHub Release**，不做 Vercel 部署
- 下一步：删除 deploy-dev/prod.yml，改造 android.yml 为 tag-triggered GitHub Release 发布流

### 涉及文件
- `.github/workflows/ci.yml` - npm ci 升级
- `.github/workflows/build.yml` - npm ci + VITE_* 变量
- `.github/workflows/deploy-dev.yml` - CI 门控 + 移除 --prod + VITE_* 变量
- `.github/workflows/deploy-prod.yml` - CI 门控 + artifact 传递 + VITE_* 变量
- `.github/workflows/release.yml` - 新增 tag 触发型 Release workflow（CI门控 → Debug APK → GitHub Release）
  - 删除了 `deploy-dev.yml`, `deploy-prod.yml`, `build.yml`（repo 定位为源码+Release，不做部署）
- `docs/ANDROID_PLAN.md` - 更新 Phase 3 说明，明确 repo 不做 Vercel 部署

### GitHub Secrets 待配置
- `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`
- `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_APP_ID`
- `VITE_GEMINI_API_KEY`, `VITE_R2_PUBLIC_URL`
- `ANDROID_KEYSTORE_BASE64`, `ANDROID_KEY_ALIAS`, `ANDROID_KEY_PASSWORD`, `ANDROID_STORE_PASSWORD`（release AAB 签名用，暂无）


### 待配置（需要用户在 GitHub Secrets 中添加）
- `VITE_FIREBASE_API_KEY`
- `VITE_FIREBASE_AUTH_DOMAIN`
- `VITE_FIREBASE_PROJECT_ID`
- `VITE_FIREBASE_STORAGE_BUCKET`
- `VITE_FIREBASE_MESSAGING_SENDER_ID`
- `VITE_FIREBASE_APP_ID`
- `VITE_GEMINI_API_KEY`
- `VITE_R2_PUBLIC_URL`
