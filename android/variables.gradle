ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    buildToolsVersion = "37.0.0"
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'

    // WearOS
    wearMinSdkVersion = 30
    wearCompileSdkVersion = 34
    wearTargetSdkVersion = 34
    // WearOS Compose versions (no separate BOM, use explicit versions in wear/build.gradle)

    // Kotlin
    kotlinVersion = '1.9.24'
    // Kotlin Compose compiler extension version (must match Kotlin 1.9.24)
    composeKotlinCompilerVersion = '1.5.14'

    // Firebase BoM
    firebaseBomVersion = '32.7.2'
}